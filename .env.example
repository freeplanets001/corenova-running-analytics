# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Gemini API key is configured via the app settings page (/settings/ai)
# No API key environment variable needed - keys are stored per-team in the database

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Web Push (VAPID keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
